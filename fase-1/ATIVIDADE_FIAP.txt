PRODUTO
 - (PK) COD_PRODUTO - INCREMENT
 - (FK) STATUS
 - (FK) CATEGORIA
 - DESCRICAO_NORMAL NOT NULL UNIQUE
 - DESCRICAO_COMPLETA NOT NULL
 - PRECO_UNITARIO NOT NULL
 - CODIGO_DE_BARRAS (VARCHAR)

STATUS_ATIVO
 - COD_STATUS 
 - DESCRICAO (A|I)

CATEGORIA
  - (PK) COD_CATEGORIA - INCREMENT
  - (FK) STATUS NOT NULL
  - DESRICAO NOT NULL UNIQUE
  - DATA_INICIO NOT NULL
  - DATA_TERMINO NULL

VIDEO
 - (PK) COD_VIDEO - INCREMENT
 - (FK) STATUS
 - DATA_VISUALIZACAO DATE TIME NOT NULL
 - CONTADOR_DE_VISUALIZACOES NULL
 - (FK) CLIENTE NULL
 - (FK) PRODUTO NOT NULL
 - (FK) CLASSIFICACAO_VIDEO

CLASSIFICACAO_VIDEO
 - (PK) CLASSIFICACAO_VIDEO INCREMENT
 - DESCRICAO_CLASSIFICACAO NOT NULL

STATUS_CHAMADO
 - (PK) STATUS_CHAMADO INCREMENT
 - STATUS (E|C|F|A|X) CHAR NOT NULL
 - DESCRICAO VARCHAR NOT NULL

CHAMADO
 - (PK) COD_CHAMADO INT(10) INCREMENT
 - (FK) CLIENTE NOT NULL
 - (FK) FUNCIONARIO
 - (FK) PRODUTO NOT NULL
 - DATA_ABERTURA_CHAMADO DATETIME NOT NULL
 - DATA_FECHAMENTO_CHAMADO DATETIME NULL
 - DATA_ATENDIMENTO DATETIME NULL
 - (FK) STATUS_CHAMADO
 - TEMPO_ATENDIMENTO TIME NULL
 - SATISFACAO INT NULL MAX(10)
 - TIPO (1,2) INT(2)
 - TEXTO_CHAMADO VARCHAR MAX(4000)

DUVIDA:CHAMADO
 - (PF) COD_CHAMADO
 - DUVIDA

SUGESTAO:CHAMADO
 - (PF) COD_CHAMADO
 - SUGESTAO
 
FUNCIONARIO
 - (PK) COD_FUNCIONARIO INCREMENT
 - NOME_FUNCIONARIO NOT NULL
 - CPF VARCHAR NOT NULL UNIQUE
 - EMAIL NOT NULL
 - CARGO NOT NULL
 - NOME_DEPARTAMENTO NOT NULL

CLIENTE
 - (PK) COD_CLIENTE NOT NULL INCREMENT
 - QUANTDADE_ESTRELAS INT NULL MAX(5)
 - (FK) STATUS_ATIVO NOT NULL
 - NUMERO_TELEFONE NULL
 - LOGIN NOT NULL
 - SENHA NOT_NULL
 - E_LOGADO BOOL NOT NULL
 - NOME_CLIENTE NOT NULL

PESSOA_FISICA:CLIENTE
 - (PK) COD_CLIENTE NOT NULL INCREMENT
 - CPF VARCHAR NOT NULL UNIQUE
 - DATA_NASC DATE NOT NULL
 - SEXO_BIOLOGICO CHAR (M|F) NOT NULL
 - GENERO VARCHAR NULL
 
 
PESSOA_JURIDICA:CLIENTE
 - (PK) COD_CLIENTE NOT NULL INCREMENT
 - DATA_FUNDACAO DATETIME NULL
 - CNPJ VARCHAR NULL
 - IE VARCHAR NULL

LEAD ** TABELA CRIADA PRA R24 **
 - (PK) COD_PRODUTO_COMERCIALIZADO NOT NULL INCREMENT
 - (FK) PRODUTO
 - (FK) VIDEO
 - (FK) CLIENTE
 - (FK) FUNCIONARIO
 - TAXA_CONVERSAO_CLIENTE INT NOT NULL (Qtd Visualiacoes do Cliente / Qtd de Vendas)
 - TAXA_CONVERSAO_PRODUTO INT NOT NULL (Qtd Videos Visto / Qtd de Vendas)

VENDA
 - (PK) COD_VENDA INCREMENT NOT NULL
 - (FK) CLIENTE
 - (FK) PRODUTO
 - QUANTIDADE INT NOT NULL